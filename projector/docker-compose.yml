version: '3.6'
services:
  mqtt_bridge:
    image: davesilva/home-automation-projector:1.2.0
    build:
      context: .
    environment:
      - BROKER_HOST=192.168.1.8
      - PROJECTOR_HOST=192.168.1.11
